{
  "name": "new_project",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "cleanup": "rm -rf .cache dist",
    "dev": "npm run cleanup && parcel src/index.html",
    "build": "npm run cleanup && parcel build src/index.html --public-url ./ --no-minify"
  },
  "keywords": [],
  "author": "Ringo Rohe",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "reset-css": "^5.0.1",
    "sass": "^1.26.10"
  },
  "dependencies": {
    "parcel-plugin-static-files-copy": "^2.5.0"
  }
}
